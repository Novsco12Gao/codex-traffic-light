<Window x:Class="CodexTrafficLight.App.DiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="诊断"
        Width="560"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F5F6F8">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="诊断"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#1F2228"
                   Margin="0,0,0,12"/>

        <TextBox x:Name="DiagnosticsTextBox"
                 Grid.Row="1"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Padding="10"
                 Background="White"
                 BorderBrush="#CED3DB"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Width="90"
                    Height="30"
                    Margin="0,0,8,0"
                    Content="复制诊断"
                    Click="CopyButton_Click"/>
            <Button Width="78"
                    Height="30"
                    Content="关闭"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
